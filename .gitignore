# Ambiente virtual Python
.venv/
.venv-paddle311/
__pycache__/
*.pyc
.pytest_cache/

# Variáveis de ambiente (contém senhas!)
.env

# Logs de runtime
*.log
*.err.log
*.out.log

# Arquivos temporários
tmp_*
scratch/
.pdx_cache/

# Uploads de usuários (não versionar evidências)
data/uploads/
downloads/

# Banco local
data/grom_ocr.sqlite

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# Modelos ONNX gerados localmente (grandes, gerados via /export-onnx)
*.onnx
*_int8.onnx

# Modelos PyTorch baixados (gerenciados externamente)
# yolov8n.pt  ← manter rastreado (pequeno, necessário p/ bootstrap)

# Datasets externos (CCPD etc.) — nunca versionar
data/datasets/
data/ccpd/
*.tar
*.tar.gz
*.zip

# Artefatos transitórios de benchmark/treino (higienização branch)
runs/
data/models/openclip_embeddings/
data/models/yolo_brcars_v1.pt
data/models/yolo_brcars_v1_training_report.json

# Relatórios e saídas efêmeras de validação local
build_openclip_embeddings_latest.txt
finetune_check*.txt
finetune_stub.txt
pytest_roadmap*.txt
pytest_osint_db.txt
resultado_osint_semantic_runtime.json
resultado_validacao_osint_runtime.json
venv_test_*.txt
train_when_ready_*.txt

# Artefatos legados de debug/execução local
curl_test_response.txt
frame_*.jpg
pytest_exit_code*.txt
pytest_output.txt
resultado_pytest*.txt
resultado_debug_video.json
resultado_video_WA0023.json
resultado_video_temporal.json
