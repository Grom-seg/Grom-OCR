# Requisitos para FastAPI backend Grom OCR
fastapi
uvicorn
python-multipart
opencv-python
pillow
paddleocr
ultralytics
onnxruntime

# ── Datasets + Semantic Search ──────────────────────────────────────────────
# OpenCLIP: busca semântica veicular (Parte II do roadmap)
# Instalar manualmente quando necessário (requer ~1 GB de modelo):
#   pip install open-clip-torch
# open-clip-torch

# Fine-tuning YOLOv8 (ultralytics já instalado acima)
# Datasets brasileiros: gpupo/brazilian-cars (clone via git), BRCars (ZIP autorizado)

